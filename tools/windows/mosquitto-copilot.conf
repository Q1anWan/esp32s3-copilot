# Copilot lab MQTT broker configuration for Windows.
# This is intended for an isolated lab LAN only.
#
# ESP32 must be able to connect to the Windows PC on TCP 1883.
# Logic decision program does not connect to MQTT; it connects to the Bridge GUI on TCP 7777.

listener 1883 0.0.0.0
allow_anonymous true
persistence false
