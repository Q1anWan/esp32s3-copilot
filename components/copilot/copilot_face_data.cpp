#include "copilot_face_data.h"

/*
 * The UI now renders a rubber-hose figure directly from two runtime states:
 * idle and speaking. This translation unit is kept so existing build files and
 * includes remain stable while the old arc-keyframe table is retired.
 */
